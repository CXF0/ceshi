# 项目全盘检查：Bug 与下一步迭代工作清单

## 一、当前高优先级 Bug（建议 1~2 周内完成）

### 1) 安全配置硬编码（严重）
- [ ] **移除硬编码数据库密码**：`server/src/app.module.ts` 中直接写了数据库账号密码，存在泄漏风险。
- [ ] **移除硬编码 JWT Secret**：`server/src/auth/auth.module.ts` 与 `server/src/auth/jwt.strategy.ts` 中写死了 secret。
- [ ] **改为环境变量注入**：引入 `.env.example`，并在 README 补齐部署说明。
- [ ] **上线前轮换密钥**：数据库密码/JWT secret 全量轮换，已发 token 失效策略一并明确。

### 2) CORS 过宽（高）
- [ ] `server/src/main.ts` 目前 `app.enableCors()` 未限制来源，应改为白名单域名。
- [ ] 静态资源 `/static/` 头部当前设置 `Access-Control-Allow-Origin: *`，需按环境限制。

### 3) 生产调试日志泄漏风险（中高）
- [ ] 前端多处 `console.log`（如 `web/src/pages/system/certification/index.tsx`）包含请求参数与 URL，可能暴露敏感信息。
- [ ] 后端启动日志输出完整服务地址与目录信息，生产环境需降级为结构化日志并脱敏。

### 4) 异常处理与类型约束不足（中高）
- [ ] 后端大量 `any` 入参，缺少 DTO + class-validator，易导致脏数据入库。
- [ ] 关键模块（合同、CRM、通知）补齐参数校验与统一异常返回格式。

### 5) 上传内容治理不足（中）
- [ ] 上传文件类型与大小策略需统一（当前上传入口分散）。
- [ ] 建议增加：扩展名 + MIME 双重校验、病毒扫描（可异步）、下载鉴权策略。

### 1.x 高优先级详细执行计划（可直接排期）

#### Day 1：冻结风险与建基线
- [ ] 建立 `security-hotfix` 分支，冻结高风险配置变更窗口。
- [ ] 使用 `rg` 全仓扫描 `password|secret|token|console.log|enableCors`，输出问题清单。
- [ ] 在项目管理工具（Jira/飞书）创建 5 个 Epic：密钥、CORS、日志、校验、上传。
- [ ] 输出《变更回滚预案 v1》：回滚命令、负责人、触发条件。

#### Day 2-3：密钥外置与轮换
- [ ] 引入配置层（ConfigService 或等价方案），删除源码硬编码密钥。
- [ ] 新增 `.env.example`（不含真实值），补齐：`DB_HOST/DB_PORT/DB_USER/DB_PASS/JWT_SECRET/CORS_ORIGINS`。
- [ ] 准备密钥轮换 SOP：生成新密钥 -> 发布新版本 -> 使旧 token 过期。
- [ ] 发布后验证：新 token 正常签发，旧 token 在约定窗口后失效。

#### Day 3-4：CORS 与静态资源策略收敛
- [ ] 后端 CORS 改为白名单数组（dev/staging/prod 分离）。
- [ ] `/static/` 的 `Access-Control-Allow-Origin` 改为按来源回写（或关闭跨域）。
- [ ] 增加预检请求（OPTIONS）回归用例。
- [ ] 安全验收：随机来源域名访问应被拒绝。

#### Day 4-5：日志治理
- [ ] 前端删除调试日志；保留必要日志时走 `logger.debug` 且仅开发环境启用。
- [ ] 后端启动与业务日志脱敏：URL、目录、用户标识、token 相关字段打码。
- [ ] 增加 `LOG_LEVEL` 与 `NODE_ENV` 联动规则（prod 默认 `info/warn/error`）。
- [ ] 验收：生产模式下不出现参数明文日志。

#### Day 5-7：DTO 校验与上传治理
- [ ] 合同/CRM/通知接口优先落地 DTO + class-validator。
- [ ] 增加统一异常过滤器：字段错误、业务错误、系统错误统一结构。
- [ ] 上传校验统一中间件：扩展名 + MIME + 大小 + 文件名规范化。
- [ ] 上传文件访问策略：公开资源与私有资源分路径，私有资源加鉴权。

#### Day 8-9：联调与灰度
- [ ] 前后端联调冒烟：登录、合同新增、回款录入、通知发布、文件上传。
- [ ] 灰度发布 10% 流量，观察 24 小时（5xx、登录失败率、上传失败率）。
- [ ] 若指标异常，按《回滚预案 v1》执行回退。

#### Day 10：验收与复盘
- [ ] 输出《高优先级修复验收报告》：问题闭环率、风险残留、下阶段建议。
- [ ] 与业务方复盘：确认对运营流程无阻断。
- [ ] 将临时脚本沉淀为 CI 检查（如硬编码扫描、日志扫描）。

### 1.y 与 AI 协作沟通脚本（可复制即用）

#### 脚本 A：让 AI 生成“可执行任务拆解”
```text
你是项目技术 PM，请基于以下目标输出“10 天执行计划”：
1) 移除硬编码密钥并完成轮换；
2) CORS 收敛为白名单；
3) 清理生产调试日志；
4) 合同/CRM/通知补齐 DTO 校验；
5) 上传策略统一（扩展名+MIME+大小+鉴权）。

输出格式要求：
- 按 Day1~Day10 列出：目标、负责人角色、输入、输出、验收标准、回滚点。
- 每天不超过 5 项任务。
- 用表格输出。
```

#### 脚本 B：让 AI 生成“代码改造清单（按文件）”
```text
请阅读该项目并仅输出“改造清单”，不要直接改代码。
要求：
- 按模块列出：auth/main/app.module/contract/crm/notification/upload。
- 每条清单必须包含：文件路径、改造点、风险等级、预估工时、是否需要联调。
- 对密钥和 CORS 相关项标记为 P0。
```

#### 脚本 C：让 AI 生成“测试与验收用例”
```text
请为以下改造生成测试清单：密钥外置、CORS 白名单、日志脱敏、DTO 校验、上传安全。
输出：
- 单元测试用例
- 接口测试用例
- 回归测试用例
- 灰度观察指标（含阈值）
每条用例需包含：前置条件、步骤、预期结果、失败后定位方向。
```

#### 脚本 D：让 AI 生成“上线通告与跨团队同步文案”
```text
请写三版文案：
1) 面向研发团队（技术细节版）
2) 面向业务运营（影响说明版）
3) 面向管理层（风险与收益版）
主题：高优先级安全与稳定性修复上线。
要求：每版控制在 250 字以内，强调变更窗口、影响范围、回滚方案。
```

#### 脚本 E：让 AI 做“复盘主持稿”
```text
请生成 30 分钟复盘会议主持稿，议题：
- 本次 P0 修复做对了什么
- 哪些问题暴露了流程缺陷
- 下一轮如何把人工检查转为 CI 自动化
输出包含：时间轴、提问清单、结论模板、行动项模板。
```

---

## 二、中优先级工程问题（建议 2~4 周）

### 6) 仓库包含运行期上传文件（中）
- [ ] `server/uploads/` 下存在大量业务文件，需立即从 Git 追踪中移除。
- [ ] 增加 `.gitignore` 规则：忽略运行期上传目录与构建产物。
- [ ] 清理历史大文件（如有必要，使用 git filter-repo）。

### 7) 前后端文档不足（中）
- [ ] 当前 `server/README.md` 与 `web/README.md` 基本仍是模板，需补齐真实业务文档。
- [ ] 增加模块边界、角色权限矩阵、部署拓扑、常见故障排查。

### 8) API 规范一致性不足（中）
- [ ] 统一分页结构与错误码。
- [ ] 统一字段命名风格（camel/snake），减少前端二次映射。
- [ ] 输出 OpenAPI/Swagger 文档，并作为联调基线。

### 9) 缺少自动化测试基线（中）
- [ ] 后端：至少为 auth/contract/crm 增加 service + controller 单测。
- [ ] 前端：关键页面（登录、合同列表、证书管理）增加组件/集成测试。
- [ ] 增加最小 e2e（登录->创建客户->创建合同）。

---

## 三、下一步迭代功能清单（按业务价值排序）

### A. 权限与审计（建议优先）
- [ ] 细粒度权限点（按钮级）统一配置中心，前后端同源。
- [ ] 审计日志：谁在何时修改了什么（合同状态、客户信息、通知发布）。
- [ ] 高风险操作二次确认（删除、状态回退、批量导入）。

### B. 合同与回款闭环
- [ ] 合同生命周期看板（待提交/审核中/执行中/已归档）。
- [ ] 回款提醒（按到期日、超期天数）+ 消息中心联动。
- [ ] 发票与附件统一归档视图，支持批量导出。

### C. CRM 精细化运营
- [ ] 客户分层标签（行业、规模、合作阶段、跟进热度）。
- [ ] 跟进记录时间轴 + 下次跟进提醒。
- [ ] 渠道转化漏斗报表（线索->商机->签约）。

### D. 仪表盘升级
- [ ] 管理层视图：收入目标达成率、回款预测、风险客户。
- [ ] 顾问视图：本周待办、到期证书、逾期合同。
- [ ] 支持自定义日期区间与数据导出。

### E. 通知系统增强
- [ ] 通知已读/未读分层策略 + 强提醒（站内+邮件/企业微信可选）。
- [ ] 富文本通知安全过滤（XSS 白名单）。
- [ ] 通知对象支持组织树批量选择与去重预览。

### F. 官网/内容运营能力
- [ ] 官网案例、服务项支持草稿/发布流。
- [ ] 图片素材中心（复用、压缩、格式转换）。
- [ ] SEO 基础能力：meta、sitemap、结构化数据。

---

## 四、建议的执行节奏（6 周示例）

### 第 1-2 周（安全加固）
- [ ] 完成密钥外置、CORS 收敛、日志降噪、上传策略统一。

### 第 3-4 周（稳定性与规范）
- [ ] 完成 API 规范统一、README 重写、基础单测与 e2e 落地。

### 第 5-6 周（功能迭代）
- [ ] 完成“合同回款闭环 + 权限审计 + 仪表盘一期”上线。

---

## 五、验收标准（Definition of Done）

- [ ] 不再存在硬编码密钥；部署仅依赖环境变量。
- [ ] 前后端核心链路具备最小可用自动化测试并纳入 CI。
- [ ] 合同与回款链路可追踪、可提醒、可统计。
- [ ] 关键管理页面具备可审计、可追责能力。
